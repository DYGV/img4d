language: d

d:
  - dmd

before_install:
  - sudo apt-get update
  - sudo apt-get install imagemagick

script:
  - cd src
  - rdmd test.d

after_success:
  - cd ../png_img
  - composite -compose difference lena.png encoded_lena.png diff.png
  - identify -format "%[mean]" diff.png
